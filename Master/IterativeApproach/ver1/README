------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------
Iterative Approach
------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------


Graph matching as an iterative approach:

graph -> coarse graph (anchor graph) -> coarse graph matching -> graph matching -> graph update


------------------------------------------------------------------------------------------------------
Ver 0
------------------------------------------------------------------------------------------------------

Initial grah of a given image:

  Use edge points of a given image as nodes of the corresponding graph 

Anchor graph of a given image:

  Extract super pixels of a given image.
  For each edge point decide to which super pixel it belongs to.
  Take center of mass of the edge points inside one super pixel as it's center.
  Nodes of the anchor graph = center of the super pixels
  
  Connect vertices that correspond to the super pixels with a common edge
  
  Cluster vertices of the original graph according to the k nearest anchor nodes.
  
Correspondece graph between two anchor graphs
  complete graph
  
Matching of the anchor graphs
  Use Reweighted Random Walk algorithm (RRW) for matching of the anchor graphs
  
Matching of the original graphs (works not so good)
  for each anchor node a_i of the first graph 
    b_j matched anchor of the second graph 
    match vertices of the initial graphs, that correpond to anchor nodes a_j and b_j
    
  for each node of the first initial graph
    select best match over all matches (max)
  
------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------
Ver 1
------------------------------------------------------------------------------------------------------

 Use hierarchical structure of super pixels to constract initial graph and corresponding coarse graph:
  for a given image img
    extract edge points
    calculate m super pixels of the selected region (???)
    use a center of mass of the edge points inside of each coarse superpixel as it's center
    anchor nodes = center of the superpixels
    conect anchor nodes if the corresponding coarse super pixel have a common edge
    
    subdivide each of m super pixels in n(m) super pixels
    center of each new finer super pixel = node of the fine graph
    conect nodes if the corresponding fine super pixel have a common edge
    
    
    
    
    